# Test Case Generation Platform — 设计方案

> 版本 v0.24 | 更新 2026-08-10 | 状态：CI + 回归测试 61 项（新增 token 用量采集）

## 修订记录

| 版本 | 日期       | 主要变更                                                                                                                                                                                                                 |
| ---- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| v0.5 | 2026-06-13 | 全功能交付：知识库管理、混合检索、用例生成、审核闭环、Excel 导入导出                                                                                                                                                     |
| v0.6 | 2026-06-30 | 生成改为后台任务（asyncio + 独立 DB 会话），SSE 事件缓存重放，切页/刷新后断点续看；前端状态提升到 Pinia store                                                                                                            |
| v0.6 | 2026-06-30 | 用例修正由「整批重写」改为「评审 - 删除 - 补充」：LLM 以测试专家身份逐条判定保留/删除，定向补充缺口，保留用例不改写                                                                                                      |
| v0.7 | 2026-07-01 | 并行生成（前端多任务）+ 多人隔离（client_id / owner_id）；新增需求补全 `POST /generate/clarify`（知识库补齐简略需求）；清理死代码（删除 `POST /generate`、`/generate/stream`）；LLM 调用容错（429 不重试、5xx 退避重试） |
| v0.8 | 2026-07-15 | SSE 新增 `knowledge` 事件：检索结束后立即推送命中知识明细，前端不用等生成/评审完成即可展示；`complete` 仍带同字段作为断线重连兜底                                                                                        |
| v0.9 | 2026-07-22 | PRD 导入支持飞书链接（Wiki 节点 / docx / 旧 doc），Docx 内表格转 GFM Markdown 保留结构；`PrdDocument` 新增 `source_type` / `source_ref`，按 obj_token 覆盖去重                                                            |
| v0.10 | 2026-07-24 | 审核阶段允许人工微调用例文案：新增 `PATCH /cases/{id}`，可改 title/precondition/steps/expected_result；编辑只改内容 + 打 `edited` 标记，不碰 review 记录（原始 reject_reason 不丢）；`TestCase` 加 `edited/edited_at` 列 |
| v0.11 | 2026-07-28 | LLM 输出解析分级容错：合法空结果（`{"cases": []}`）透传原因而非「格式错误，请重试」；截断输出逐条抢救；思考无正文提示调参。「无有效用例」统一按 `error` 事件处理、不发 `complete`、不落库；`persist_cases` 跳过无 title/error 占位 |
| v0.12 | 2026-07-29 | 分批生成融合架构：阶段1抽取模块清单 → 阶段2按模块分批生成（每批内部套续写兜底）→ 阶段3跨批去重合并。`finish_reason=length` 检测截断后自动续写，`LLM_MAX_CONTINUATIONS` 兜底上限。新增 `LLM_ENABLE_MODULE_SPLIT` 开关可一键回退。
| v0.13 | 2026-07-29 | 评审补充用例就近归组：补充用例按标题【】顶层模块插入到同模块用例之后，而非一律追加末尾，避免补充用例脱离相关模块影响观感（`_merge_supplements`）。
| v0.14 | 2026-07-29 | 模块**并行**生成（替代 v0.12 的串行 for）：`LLM_MODULE_CONCURRENCY` 并发上限 + `LLM_MODULE_STAGGER_DELAY` 错峰启动，各模块独立 `LLMService` 实例，`asyncio.Queue` 汇流单点转发。模块内改**真流式**（`generate_stream` + `on_chunk` 回调），SSE 新增 `module_start/module_chunk/module_done/module_failed` 事件，前端按 agent 卡片分区展示各自的流（可多开，完成后卡内换为解析好的用例列表）。评审改**分批并发**（`LLM_REVIEW_BATCH_SIZE`，index 偏移映射回全局）防大批次超时；`generate_stream` 超时改「空手超时才退避重试」，429 纳入退避重试集。
| v0.15 | 2026-07-30 | 生成耗时可视化：后端 `time.monotonic()` 记录每个模块生成耗时（不含错峰/排队等待，随 `module_done`/`module_failed` 事件带 `elapsed` 秒）与整体总耗时（随 `complete` 事件带 `elapsed`），前端在各 agent 卡片标题旁显示 ⏱ 耗时、结果区标题显示总耗时。
| v0.16 | 2026-07-30 | 评审/补充改**按模块并行 + 流式卡片**：评审按【模块】分组，每个模块一个独立评审 agent 并行跑，SSE 新增 `review_start/review_thinking/review_chunk/review_done/review_failed` 事件，前端 `reviewAgents` 卡片区实时展示「AI 正在保留/删除哪条及其理由」。补充按被删模块分组 + 遗漏场景单独一组，各一个补充 agent 并行流式生成，SSE 新增 `supplement_*` 事件，前端 `supplementAgents` 卡片区实时展示。共用生成阶段同一套 `Semaphore`+错峰限流与 `asyncio.Queue` 汇流机制（`_parallel_agents` 通用运行器）。取代 v0.14 的评审分批并发（`LLM_REVIEW_BATCH_SIZE` 阈值 300 实际几乎不触发、且非流式看不到过程）。|
| v0.17 | 2026-08-04 | 用例顺序细化到**子功能级**：`_merge_supplements` 的最长公共前缀就近插入只保证顶层模块聚合，同模块内不同子功能仍可能交错（补充用例落在模块块尾部）。新增运维脚本 `backend/scripts/resort_batch.py`，对已落库批次重排 `created_at`（列表接口按其升序返回）：先按顶层模块切连续块保持切割顺序，块内按标题【】层级路径做层级化稳定排序（每级次序 = 该级路径在块内首次出现位置）。|
| v0.18 | 2026-08-04 | 用例顺序再细化到**功能点级**：路径最后一级往往只到页面/区块（「提交页」「统计概览」），真正的功能点（「现场照片」「本周走访」）写在标题正文里，路径排序排不到一起。`resort_batch.py` 在同路径段内追加**单向前移**聚合（`_forward_place`）：剥掉正文开头套话动词后比对共同前缀，≥3 字判为同功能点，插到该簇末尾；已排定顺序永不改动。单向是关键——双向重排会把总述型用例（「验证走访类型单选（4类）」）挤到细则之后，而总述天然先于细则出现，只允许后来者前插即可守住。阈值 3 有实测依据：真同功能点用例对共同前缀最少 3 字，而「点击X跳转并筛选」这类跳转句式的跨功能点误配最多 2 字，可干净分开（放宽到子串匹配会因「点击/跳转/筛选」通用词误聚）。|
| v0.19 | 2026-08-04 | 驳回原因 `discard`（「丢弃」）改为 `duplicate`（「重复」）：value 与 label 一并改，因库中 0 条 `discard` 记录、且只有单一环境，无需迁移与兜底映射；后端 `reject_reason` 是 `String(50)` 自由文本、无枚举校验，故只动前端三处（`types/review.ts` / `ReviewView` / `StatsView`）。历史批次的「下载 Excel」改为 split-button，可选「全部用例 / 仅通过用例」——后者筛 `review.status === 'approved'`（只认审核动作，与 `edited` 无关），筛空时提示而非下载空文件，文件名加 `_已通过` 后缀区分。生成页不加该选项：那里用例尚未入库、没有人工评审状态。|
| v0.20 | 2026-08-04 | 补充用例可标识：`TestCase` 新增 `origin` 列（`VARCHAR(20)` 可空，`'supplement'` = 评审后针对被删/遗漏场景定向补充），迁移沿用 `database.py` 既有 pragma 幂等方式（`_migrate_test_case_origin_column`）。打标位置在 `_merge_supplements` **之前**的去重收口处——合并后补充用例会散入各自模块，事后无法再区分（生成/补充的 `source` 同为 `'ai'`，`created_at` 只是写库时间）。经 `persist_cases` 落库、`GET /cases` 与 SSE `complete` 事件返回，前端在生成结果区/历史批次/审核页显示「补充」标签。加列前的历史用例一律 `NULL` 且不显示标签：那批数据的阶段信息在落库时已丢失，反推不可靠，不把猜测当事实。|
| v0.21 | 2026-08-04 | 生成时**自动排序**，不必再手工跑 `resort_batch.py`：排序规则提到 `app/utils/case_ordering.py` 与运维脚本共用（函数接 `title_of` 回调，故既能排 ORM 对象也能排 dict），避免两处各写一份、改一边漏一边。生成流程在补充合并之后、`validate_cases` **之前**调 `order_cases`——前者保证补充用例参与归位，后者保证告警的 `case_index` 与最终顺序对齐。**只挪补充用例**（`is_movable=origin=='supplement'`），原有用例位置一律不动：用重排前的备份库统计，LLM 原始产出里 350 个子功能路径仅 6 个被拆段（涉及 71/1082 条），生成阶段本就整齐；而功能点判定靠字面共同前缀这一启发式，会把「点击X跳转并筛选」这类同句式的跨功能点用例误吸到一起，动原有用例收益小、风险大。实测：无补充用例时 0 条位置变化，有补充时原有相对顺序不变、顶层块序不变、幂等。另治本一手——生成 prompt 增加硬约束「前缀最后一级必须是功能点，不能停在页面/区块名」并给正反例（❌`【提交页】验证现场照片添加与删除` → ✅`【提交页-现场照片】数量上限9张边界值`），补充 prompt 要求复用已有功能点的完整前缀；路径自带功能点后，字面前缀启发式退化为兜底。|
| v0.22 | 2026-08-05 | 修正 v0.21 的 `is_movable` 只拦住第 3 层：第 2 层的路径层级排序（`sort_block_by_path`，原 `resort_block` 内联）是**全量重排**，会把块内交错的同路径用例并段——`【模块-登录】【模块-注册】【模块-登录】` 三条原有用例即便一条补充都没有，也会被重排成登录、登录、注册。这与「原有用例位置一律不动」的承诺相悖。现拆出 `place_by_path`：锁定用例按原序构成骨架、一条都不排，可动用例再逐条按路径最长公共前缀插到所属簇尾（`len_common_segs`）；`resort_block` 据 `is_movable` 二选一——不传走全量排序（运维脚本补排历史批次的语义不变），传了走只插不排（生成流程）。吸附力用 `_affinity` 打二元组分而非单看公共层数：光比公共前缀分不出「路径完全相同」和「我是你的祖先」——总纲 `【提交页】` 与细则 `【提交页-日常走访】` 对一条 `【提交页】` 补充的公共层数都是 2，同分取更靠后就把补充推到了细则之后，**总纲与总纲被细则隔开**（正是 v0.18 单向前移要避免的倒置，只是这次发生在路径层）。故把 supp 的后代降一档，补充遂紧跟同级总纲之后。实测：整批模式在两个库上与重构前**逐条 ID 完全一致**（脚本 dry-run 对已排好的库仍报 0 变化）；锁定模式零补充时 0 挪动，48 个锚点 × 3 个插入位置注入补充、以及 20 组 3~20 条随机补充的压力测试中，原有用例顺序打乱 0 次、补充精确落到同路径簇尾 48/48、集合一致且幂等；400 次单条补充注入中「补充紧跟同级用例」400/400（修 `_affinity` 前此项有反例）。另更正 v0.22 初版的一处论断：曾以「补充用例可能被 `_merge_supplements` 插到所属子模块首条之前」论证必须先摆完整骨架，实测 200 次单条补充**全部**落在簇末之后、0 次落在簇首之前，且骨架版与边遍历边插版在 300 组随机补充上输出完全相同——该理由不成立，已从 docstring 移除（保留先摆骨架的写法，因其与流式版等价而更易读）。|
| v0.23 | 2026-08-05 | 补 CI（GitHub Actions）与回归测试 43 项。**为让测试摆脱向量库依赖**，把 `_title_prefix`/`_title_path`/`_common_prefix_len`/`_merge_supplements` 从 `generator_service` 抽到 `app/utils/case_grouping.py`（公开命名去掉下划线）：`generator_service` 顶部 import ChromaStore，测试一 import 就连带拉起 chromadb（实测约 433 MB / 80 秒），而这几个函数只做字符串与列表处理。抽出后 CI 只装 `pytest`（32 MB / 5 秒），测试耗时从 0.45s 降到 0.02s。搬迁**未合并**`case_grouping.title_path` 与 `case_ordering.title_segs`——两者对空段的处理不同（`【A--B】` → `['A','B']` vs `('A','','B')`），是归位与排序各自的既有行为，合并等于悄悄改掉一条；已补测试钉住该差异。等价性用真实数据验证：1091 个标题（含 1082 条真实用例）、2000 组随机路径、60 组随机补充，与旧实现**逐条零差异**。测试有效性用变异测试验证：注入「顺手统一空段语义」「同分不取更靠后」「就地改传入列表」三个 bug，各被 2 项测试抓住。CI 跑 push main 与 PR：后端 pytest（Py3.10 取版本下限）、前端 `npm ci && npm run build`（Node18，`build` 含 vue-tsc 类型检查）；并发组设 cancel-in-progress 避免连续 push 排队。|
| v0.24 | 2026-08-10 | **Token 用量统计**：新增 `llm_usage` 流水表（一行 = 一次 chat/completions 请求，含续写每轮与各并行模块），统计分析页展示今日/本周/累计消耗、思考 token 占比、按阶段拆分条形图，审核页批次卡显示该批消耗。采集靠流式请求带 `stream_options.include_usage`（`LLM_COLLECT_TOKEN_USAGE` 可一键关，应对个别服务商对未知字段报 400）。两处实现要点：① **usage 必须在取 `choices[0]` 之前读**——带 usage 的那个 chunk 里 `choices` 是空数组，先取下标会抛 `IndexError` 被既有的 `except (JSONDecodeError, KeyError, IndexError)` 吞掉，usage 永远采不到；② 归属靠 **contextvars** 而非改调用签名：`LLMService()` 在 clarify/模块拆分/生成/评审/补充 5 处各自 new，其中 3 处跑在 `create_task` 起的并发 worker 里，透传 stage 要改遍所有 worker 签名与 `_parallel_agents` 协议；`create_task` 复制创建时的上下文，故在 `task_service._run` 顶层装一次 `collector()` 就能收齐全部并发调用，而各 worker 内 `stage()` 只改自己的副本、并发下不串台（已用交错 await 的并发测试钉住）。落库时机在生成结束、拿到 `batch_id` 后一次性 flush（批次级消耗靠它），且放在 `async_session` 内层的 `finally`——**失败路径也记账**，token 已经花掉了，撞限流前那几轮的消耗更该被看见。统计失败绝不连坐生成：`flush` 整体 try 住，出错只打日志。时间口径沿用 `now_local()`（naive Asia/Shanghai），与 `test_cases.created_at` 一致，「今日/本周（周一起）」直接按日期比较无需时区换算。功能上线前的历史批次没有流水，`tokens` 返回 `null` 而非 0，前端不显示——「0 tokens」会被读成「这批没花钱」，是错的。**模块分工**：采集逻辑（contextvars 收集器、usage 解析、时间边界）在 `app/utils/token_usage.py`，落库与聚合在 `services/usage_service.py`，后者 re-export 前者的公开 API 使调用方无需关心拆分。这层拆分不是洁癖而是 CI 的硬约束——CI 只装 `pytest`（避开 chromadb 与 torch），采集若与 sqlalchemy 同处一个模块，测试一 import 就 `ModuleNotFoundError`（首版正是如此挂掉的），与 v0.23 抽 `case_grouping` 同一动因。|

> 后续每次修改设计时，在此表追加一行（版本、日期、主要变更）。


## 1. 系统架构总览

```
┌─────────────────────────────────────────────────────────┐
│               Web 前端 (Vue 3 + Element Plus)            │
│  首页看板 │ 用例生成 │ 审核标注 │ 知识库(卡片式)              │
└────────────────────────┬────────────────────────────────┘
                         │ REST API
┌────────────────────────┴────────────────────────────────┐
│               后端服务 (Python/FastAPI)                   │
│  ┌───────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 知识库CRUD│ │ 检索(混合)│ │ LLM生成  │ │ 审核统计  │ │
│  └───────────┘ └──────────┘ └──────────┘ └──────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  六大测试技术Prompt (等价类/边界值/决策表/       │   │
│  │  状态迁移/错误推测/组合测试) + 覆盖率总结        │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │  知识检索(关键词+ChromaDB向量) + 评审补充        │   │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                     数据层                                │
│  ┌──────────┐ ┌──────────┐ ┌────────────────────────┐   │
│  │  SQLite  │ │ ChromaDB │ │    LLM API (OpenAI兼容)       │  │
│  │(知识库)  │ │ (向量库)  │ │                         │  │
│  └──────────┘ └──────────┘ └────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 核心设计原则

**知识先行，Prompt后行** — 任何用例生成请求，必须先经过知识检索，将检索结果注入Prompt。不允许直接"裸调"LLM。

**可追溯** — 每条生成的用例都要记录它引用了哪些知识条目。用户审核时能回溯"这个预期结果是从哪条业务规则推导出来的"。

**瘦启动** — 先用SQLite、单进程跑通闭环，后期再根据需要切PostgreSQL、加Redis缓存、做服务拆分。

---

## 2. 核心模块设计

### 2.1 知识库管理模块（系统基石）

知识库是整个平台的护城河。设计中定义了 **7种知识类型**（4种结构化 + 3种向量化）：

#### 类型1：字段字典
```
字段名称 | 页面展示名 | 数据来源 | 类型 | 合法取值/枚举 | 业务含义 | 所属模块
channel_count | 设备数    | camera表  | int  | ≥0           | 一个摄像机可含多通道，页面写"设备数"实际是"通道数" | 实时视频
```
**用途**：根治"字段幻觉"——生成用例前，对照字段字典校验每个引用的字段是否存在、类型是否正确。

#### 类型2：业务规则
```
规则名称 | 规则表达式                | 规则类型 | 所属模块 | 来源
VIP免运费 | user_level='vip' → freight=0 | 硬规则   | 订单模块 | PRD v3.2 + 代码逻辑
```
规则类型分：
- **硬规则**：代码强制约束（如状态机、字段取值）
- **软规则**：业务惯例（如"大促期间优惠券不可叠加"）

**用途**：根治"规则幻觉"——约束LLM只能在规则允许的空间内生成用例。

#### 类型3：状态机
```
实体   | 状态流转                     | 所属模块
订单   | 待支付→已支付→已发货→已完成  | 订单模块
订单   | 待支付→已取消                | 订单模块
订单   | 已支付→已退款→已关闭         | 订单模块
```
**用途**：根治"上下文缺失"——生成用例时注入状态机，让LLM知道前置状态依赖。

#### 类型5：字段-术语映射
```
页面术语 | 技术字段      | 映射关系     | 所属模块
设备数   | channel_count | 1摄像头=N通道 | 实时视频
```
**用途**：解决"设备数 vs 通道数"这类命名歧义问题。

#### 类型6：PRD文档库（文件上传 + 向量化）

支持上传 PDF/Word/Markdown/TXT 格式的 PRD 文档，自动文本提取后分块向量化存入 ChromaDB。生成用例时，从 PRD 库中检索最相关的文档片段注入 Prompt，让 LLM 具备完整业务上下文。

```
文件名称 | 所属模块 | 文件格式 | 提取文本 | 向量化分块 | 上传时间
```

**用途**：根治"上下文缺失"——PRD 里有详细业务描述和隐含规则，直接检索注入比人工提炼更完整。

#### 类型7：缺陷记录库（历史Bug）

```
缺陷ID | 标题 | 严重级别 | 根因分类 | 描述 | 关联模块 | 关联用例 | 发生时间
```

每条缺陷的描述文本向量化存入 ChromaDB。生成用例时，检索历史上最相关的缺陷记录，注入 Prompt 让 LLM 知道"**这里曾经出过什么问题**"，针对性地生成回归测试用例。

**用途**：根治"历史遗漏"——老测试知道哪些地方容易出 Bug，现在 AI 通过缺陷记录也能知道。

---

### 2.2 用例生成模块（核心引擎）

生成流程（v0.12 移除分批融合架构）：

```
用户输入需求
  ├─ 粘贴文本 ──→ 直接进入流程
  └─ 上传PRD文件 ──→ 格式检测+文本提取 → 可选LLM关键信息识别 → 进入流程
       │
       ▼
┌──────────────────┐
│ 知识检索           │ ← 混合检索：关键词匹配（字段字典/业务规则/状态机）
│                    │            + 语义检索（历史用例向量相似度 Top-K）
└──────┬───────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ 【阶段1】模块拆分（可选）               │ ← LLM_ENABLE_MODULE_SPLIT 开关
│ 抽取需求中的模块/功能域清单            │    需求 < MODULE_SPLIT_MIN_CHARS 时跳过
│ 自报 covers_all=false 时防漏回退       │    失败/≤1个模块 → 退化为单批
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│ 【阶段2】按模块并行生成 + 续写兜底     │ ← 并发上限 LLM_MODULE_CONCURRENCY
│  并发(错峰 STAGGER_DELAY 启动):        │    各模块独立 LLMService 实例
│    生成(module_focus=模块, 真流式)     │    on_chunk→asyncio.Queue 汇流单点转发
│    若 finish_reason=length:            │    → SSE module_start/chunk/done/failed
│      续写(带已有title防重复)           │    前端按 agent 卡片分区展示各自的流
│      循环到 stop 或 上限3轮             │    单模块失败只跳过该模块，不中断整批
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────┐
│ 跨批去重           │ ← 按 title 归一化后精确匹配，保序保留首次（并行后统一去重）
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 自动校验           │ ← 引用字段/业务规则/状态机一致性
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ LLM 评审(按模块并行)│ ← 逐条 keep/delete，不改写；按【模块】分组各一 agent 并行 + 流式卡片
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ LLM 补充(按模块并行)│ ← 被删场景按模块 + 遗漏场景各一 agent 并行流式生成，统一去重后合并
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 后处理 + 追溯注入  │ ← 为每条用例打上知识引用标签
│                    │   去掉冗余/重复项
└──────┬───────────┘
       │
       ▼
   用例结果列表
```


#### Prompt 构造策略（三层结构）

```
[System Layer]
你是一个专业的功能测试用例设计专家。你必须严格基于以下知识库信息生成用例，不得编造任何字段、规则或状态：

## 可用字段字典
{table: field_dictionary}

## 强制业务规则（必须遵守）
{table: business_rules}

## 状态机流转（不可违反）
{table: state_machines}

## 术语映射（注意不要被页面命名误导）
{table: term_mapping}

[Context Layer]
## 相似历史用例（供参考格式和粒度）
{retrieved_historical_cases}

[User Request Layer]
{用户输入的需求描述}

输出要求：每条用例包含 模块、场景、用例标题、前置条件、测试步骤、预期结果，以 JSON 数组格式输出。

步骤粒度按用例复杂度分三档，避免为凑数写废话步骤（详见 `prompt_service.SYSTEM_TEMPLATE`）：
- **A 档 极简型**（查询/展示/静态校验）：precondition/steps 可写 "-"，验证点直接写进 expected_result
- **B 档 单操作型**（一步操作+验证）：steps 一句话，不拆"进入页面→点按钮"
- **C 档 多步流程型**（表单/状态流转/复杂交互）：steps 用 1./2./3. 编号，expected_result 用相同编号引用对应步骤，允许某步无预期

通用禁止项：不把"登录/进入页面/打开菜单"作为独立步骤（合并到 precondition 或省略）；不写"页面加载成功""按钮可点击"这类无验证意义的预期。
```

#### 生成任务的可靠性设计（后台任务 + 断线重连）

生成是耗时长任务（检索 + LLM 流式 + 评审补充可达数十秒），不能绑定在单次 HTTP 请求或某个前端页面组件的生命周期上。设计为：

```
前端点击生成
   │  POST /generate/async
   ▼
后端 TaskManager.create() ──→ asyncio.create_task（独立 DB 会话）
   │  立即返回 task_id          后台任务持续运行，不受客户端断开影响
   ▼                                  │
前端凭 task_id 订阅 SSE                 │ 边生成边把事件缓存进 task.events
   GET /generate/stream/{task_id}      ▼
   ├─ 先重放已缓存事件（断点续看）   生成完成时落库（persist_cases）
   └─ 再接收后续实时事件
```

- **任务脱离请求**：后台任务用独立 DB 会话运行，前端切页面/刷新/关闭标签页都不会中断生成，结果照常落库。
- **事件缓存 + 重放**：每个任务把所有 SSE 事件缓存在内存中，重连时先重放历史事件、再续接实时流，实现「刷新后断点续看」。
- **活动任务发现**：`GET /generate/active?client_id=` 按归属者列出运行中的任务，前端在应用加载时据此自动重连本客户端的所有任务，并在全局页头展示可点击的「生成中」入口。
- **内存治理**：已完成任务按 TTL（默认 1 小时）与数量上限（默认 50）淘汰，避免长时间运行内存膨胀。
- **已知边界**：任务注册表为进程内内存态，后端进程重启会丢失「活动任务」列表（已落库的用例不受影响）；如需跨重启续看，需将任务状态持久化到 DB。

> 前端状态同样从视图组件提升到 Pinia store（`stores/generation.ts`），生成状态（进度、流式文本、结果）与页面解耦，切换 tab/路由均不丢失。

#### 需求补全（知识库补齐简略需求）

需求描述往往比较简略，直接生成容易遗漏隐含逻辑。为此在生成前提供一个可选的「需求补全」步骤：

```
原始需求（可能简略）
   │  POST /generate/clarify
   ▼
检索知识库 → LLM 补全为结构化完整需求（Markdown）
   │  返回 clarified_text
   ▼
前端展示可编辑文本框 → 用户确认/修改
   │
   ▼
以补全后的需求作为生成输入 → 后台生成任务
```

- 补全产出结构化需求，覆盖：字段及取值约束、必须遵守的业务规则、相关状态流转、异常与边界场景、历史缺陷对应的回归风险点。
- 只依据知识库补充，**严禁编造**知识库中不存在的字段/规则/状态；对补充内容标注「（补充）」，用户可识别与修改。
- 补全只做「补需求」，不生成用例；补全 ≠ 把知识库内容当需求，需求主体仍是用户输入。
- 该步骤可选：需求已足够详细时可直接生成。

#### 生成后评审（保留-删除-补充，非整批重写）

生成完成后，由 LLM 以测试评审专家身份**逐条判定保留/删除**（引用错字段、违反规则、重复、含糊不可执行、偏离需求才删），保留的用例**不改写**。评审按【模块】分组并发，每个模块一个独立 agent 并行跑，SSE 流式实时吐出「AI 正在保留/删除哪条、理由是什么」，前端 `reviewAgents` 卡片区展示。再针对被删场景与遗漏场景**定向补充**新用例——被删场景按模块分组 + 遗漏场景单独一组，各一个补充 agent 并行流式生成，前端 `supplementAgents` 卡片区展示；跨 agent + 与保留用例统一按 title 精确去重后合并。补充用例按标题【】里的顶层模块**就近插入**到同模块用例之后（而非一律追加到末尾），保证同模块用例挨在一起；找不到同模块的新模块补充仍追加末尾。避免早期「整批重写」把合格用例一并改掉。

**用例顺序（功能点级聚合）**：排序规则集中在 `backend/app/utils/case_ordering.py`，由生成流程与运维脚本**共用**（函数接 `title_of` 回调，故既能排 ORM 对象也能排 dict）。生成阶段在补充合并之后、`validate_cases` 之前调 `order_cases` 自动排好，落库即是最终顺序；`backend/scripts/resort_batch.py` 用于补排该逻辑之前落库的历史批次（列表接口按 `created_at` 升序返回，脚本即按目标顺序重写 `created_at`，原最早时间起逐条 +1 秒）。规则三层——① 先按顶层模块切成连续块，块的先后**不变**（顶层顺序来自需求切割的 modules 列表，是人给定的阅读顺序；生成流程里 `all_cases` 已按模块下标拼接，故只需识别块边界）；② 块内按标题【】的层级路径归位，于是补充用例归到自己所属子功能那一段——这一层按 `is_movable` 二选一：不传走 `sort_block_by_path`（全量层级稳定排序，每级次序取该级路径在块内首次出现位置，运维脚本补排历史批次用），传了走 `place_by_path`（锁定用例按原序构成骨架、一条都不排，可动用例按 `_affinity` 吸附力插到所属簇尾——第一维是路径公共层数 `len_common_segs`，第二维压低 supp 的后代，否则总纲级补充会被自己的细则隔开，生成流程用）；③ 同路径段内再按**功能点**聚合（`forward_place`）——路径最后一级常只到页面/区块（「提交页」），功能点（「现场照片」）写在正文里，故剥掉正文开头套话动词（验证/校验/确认…）后比对共同前缀，≥`MIN_PREFIX`(3) 字判为同功能点、插到该簇末尾。第 2、3 层都必须尊重 `is_movable`：只在第 3 层拦是不够的，路径排序自己就会重排锁定用例（见 v0.22）。

**测试与 CI**：`backend/tests/` 43 项回归测试（`test_case_ordering` 17 项按「承诺」组织断言，`test_case_grouping` 26 项覆盖前缀解析与就近插入），只装 `pytest` 即可跑、0.02s 完成。为此把补充用例的就近插入（原 `_merge_supplements` 等）从 `generator_service` 抽到 `backend/app/utils/case_grouping.py`——`generator_service` 顶部 import `ChromaStore`，测试一 import 就连带拉起 chromadb（约 433 MB），使这段纯字符串逻辑无法轻量测试。注意 `case_grouping.title_path` 与 `case_ordering.title_segs` **语义不同**（前者丢弃 `【A--B】` 的空段、后者保留），分属归位与排序两条既有链路，勿合并；已有测试钉住该差异。CI 见 `.github/workflows/ci.yml`：push main 与向 main 提 PR 时跑后端 pytest 与前端 `npm ci && npm run build`（后者含 `vue-tsc` 类型检查）。

第 ③ 步有两道约束。**只前移不后移**：已排定顺序永不改动，因为总述型用例（「验证走访类型单选（4类）」）天然先于细则出现，只允许后来者前插就能守住它的位置；双向重排则会把总述挤到细则之后。**生成流程只挪补充用例**（`is_movable=origin=='supplement'`），原有用例位置一律不动——LLM 原始产出里 350 个子功能路径仅 6 个被拆段（71/1082 条），生成阶段本就整齐，而字面前缀是启发式、会把「点击X跳转并筛选」这类同句式的跨功能点用例误吸到一起，动原有用例收益小风险大；运维脚本补排历史批次时不传该参数（整批一起整理）。阈值 3 来自实测——真同功能点最少 3 字共同前缀，跨功能点的同句式最多 2 字。局限：只看正文开头，若功能点名不在句首（「照片大小…」⇔「单张照片大小…」）仍聚不到一起，需人工微调标题；放宽到子串匹配会因通用词误聚，故不做。

**治本一手**：生成 prompt 增加硬约束「前缀最后一级必须是功能点，不能停在页面/区块名」并给正反例（❌`【提交页】验证现场照片添加与删除` → ✅`【提交页-现场照片】数量上限9张边界值`），判断标准是「同一前缀下的用例应都在测同一个输入项/按钮/展示项」；补充 prompt 要求复用已有功能点的完整前缀。路径自带功能点后，第 ③ 步的字面前缀启发式退化为兜底——prompt 依赖模型遵守、做不到 100%，故兜底保留。脚本用法 `python -m scripts.resort_batch <batch_id> [--dry-run]`（dry-run 以 ★ 标出位置变化及原下标）。

#### 并行生成与多人隔离

- **并行**：后台任务本就无全局锁；前端 store 以 `taskId -> 任务状态` 的 Map 承载多任务，可同时发起多个生成互不阻塞，任务列表可切换查看各自进度/结果。
- **归属者隔离**：请求携带 `client_id`（前端 localStorage 匿名标识），后端任务记录中立的 `owner_id`，`GET /generate/active` 按 owner 过滤。不同浏览器/用户互不串扰。
- **账号化预留**：业务只认 `owner_id`，将来接入登录只需把其来源从 client_id 换成 user.id，隔离逻辑无需改动。

#### 模型调用容错

- LLM 调用对 **429（限流/额度超限）不重试**，立即返回明确提示（重试无益，避免白等）；对 **502/503/504（服务端瞬时抖动）保留指数退避重试**（最多 3 次，2/4/8 秒，遵循 `Retry-After`）。
- **输出解析分级容错**：用例解析（`_parse_cases`）区分三类失败并给出各自可行动的提示，不再笼统报「格式错误，请重试」——① 只输出思考未产出正文 → 提示调低 `reasoning_effort` 或调大 `max_tokens`；② 被 `max_tokens` 截断的未闭合 JSON → 逐个抢救已完整的用例保住结果；③ 模型合法返回空结果（`{"cases": []}`，多因需求无可测功能点）→ 从 `gaps`/`coverage.gaps` 提取原因透传给用户，引导补充需求描述。
- **「无有效用例」按失败处理，不落库**：`generate_stream` 解析后若无任何有效用例（解析失败 / 合法空结果 / 只思考未输出），发 `error` 事件并 `return`，**不发 `complete`**——前端显示明确原因（而非「成功，共 1 条」），且后台任务不触发 `persist_cases`，避免脏记录入库。`persist_cases` 另加一道防线：跳过无 `title` 或含 `error` 的占位对象，双重保证历史里不出现空用例。

---

### 2.3 审核与反馈闭环模块

```
生成用例列表
     │
     ▼
逐条人工审核 ──→ 标注 [可用] ──→ 进入可用用例池
     │
     ▼
  标注 [不可用] ──→ 选归因类型：
                    │ 字段幻觉 → 提示补充字段字典
                    │ 规则幻觉 → 提示补充业务规则
                    │ 上下文缺失 → 提示补充状态机
                    │ 风格不一致 → 标记为反例
                    │
                    ▼
              系统记录归因 → 引导用户补充对应知识条目
                              │
                              ▼
                    知识库增量更新 → 下次生成质量提升
```

**关键指标自动计算**：
- 可用率 = 标注[可用]数 / 总用例数
- 幻觉率分布 = 各类归因占比
- 知识库覆盖度 = 被引用的知识条目 / 总知识条目

---

### 2.4 PRD 文件解析模块

用户可以直接上传PRD文档，系统自动提取文本内容作为生成输入。支持格式：

| 格式           | 解析方式            | 说明                             |
| -------------- | ------------------- | -------------------------------- |
| PDF            | PyPDF2 / pdfplumber | 提取文本内容，保留段落结构       |
| Word (.docx)   | python-docx         | 提取段落、表格                   |
| Markdown (.md) | 直接读取            | 保留标题层级，用于后续按章节生成 |
| TXT            | 直接读取            | 纯文本输入                       |

```
用户上传 PRD 文件 (PDF/Word/MD/TXT)
         │
         ▼
┌────────────────────┐
│ 格式检测 + 文本提取  │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ 关键信息自动提取     │ ← 从PRD中识别模块名、字段名、规则描述、状态流转描述
│ (可选：LLM辅助解析)  │   可作为知识库候选条目提示用户录入
└────────┬───────────┘
         │
         ▼
  提取后的文本 + 用户确认 → 进入用例生成流程（关联知识库检索）
```

**与粘贴文本的区别**：上传PRD文件后，系统可以额外做「PRD关键信息提取」——用LLM从PRD中自动识别出可能涉及的字段、规则线索，与已有知识库做对比，如果有知识库未覆盖的，给出补充建议。这个环节不阻塞流程，是可选的增强步骤。

#### 从飞书导入 PRD（v0.8）

除了本地文件上传，还支持粘贴飞书链接一键导入，避免"下载 → 上传"两步走：

| 链接形式       | 处理策略                                                                             |
| -------------- | ------------------------------------------------------------------------------------ |
| `/wiki/{token}` | 先调 `wiki/v2/spaces/get_node` 解析出真正的 `obj_type` 与 `obj_token`，再走对应支路 |
| `/docx/{token}` | 调 `docx/v1/documents/{id}/blocks` 拉出 block 树，转成 GFM Markdown（含表格）        |
| `/docs/{token}` | 走旧版 `doc/v2/{token}/raw_content`，只拿纯文本（表格会被拍平）                       |

**表格处理**：飞书 Docx 里的原生表格（`block_type=31/32`）会按行列拼成 GFM `| ... |` 语法并转义单元格里的 `|` 与换行，尽量保留结构语义；图片、电子表格、多维表格暂不支持，遇到会明确报错。

**去重策略**：以 `(kb_id, source_ref=obj_token)` 为唯一键。重复导入同一份飞书文档不会新增记录，而是**覆盖 raw_text 并重建向量索引**，避免知识库出现重复片段。列表页用 tag 区分「本地」/「飞书」两种来源。

**权限依赖**：需在飞书开放平台创建自建应用、申请 `wiki:wiki:readonly` + `docx:document:readonly` 等权限，并在目标知识空间把应用加为成员。凭证通过 `FEISHU_APP_ID` / `FEISHU_APP_SECRET` 环境变量注入。

---

### 2.5 Excel 导入/导出模块

**导入**：支持三种Sheet模板
| Sheet名  | 内容                         | 说明           |
| -------- | ---------------------------- | -------------- |
| 字段字典 | 字段名/页面名/类型/枚举/含义 | 必选           |
| 业务规则 | 规则名/表达式/类型/来源      | 必选           |
| 历史用例 | 标题/前置/步骤/预期/模块     | 可选但强烈推荐 |

**导出**：生成用例导出为Excel，兼容团队现有模板格式（需用户提供样例模板）。

---

## 3. 技术选型建议

| 层         | 选型                               | 理由                                                                                 |
| ---------- | ---------------------------------- | ------------------------------------------------------------------------------------ |
| 前端       | Vue 3 + Element Plus               | 生态成熟、表格/表单组件丰富、适合B端、用户指定                                       |
| 后端       | Python + FastAPI                   | 与AI/LLM生态无缝对接、开发效率高                                                     |
| 关系数据库 | SQLite（开发）→ PostgreSQL（生产） | SQLite零配置起步；PostgreSQL支持pgvector扩展                                         |
| 向量数据库 | ChromaDB 1.5.x                     | 轻量、Python原生、适合嵌入式场景（1.x 修复了 0.5.x 与 posthog 7.x 遥测不兼容的报错） |
| LLM SDK    | 自封装调用（OpenAI 兼容格式）      | 与Qwen API格式一致，方便切换                                                         |
| Excel处理  | openpyxl                           | Python Excel处理的事实标准                                                           |
| PRD解析    | pdfplumber + python-docx           | PDF文本提取 + Word文档解析                                                           |
| 部署       | Docker + docker-compose            | 一键启动前后端+数据库                                                                |

**为什么不选LangChain/LlamaIndex**：MVP阶段逻辑清晰，自建检索+Prompt构造足够，引入框架反而增加调试成本和复杂度。后续如果需要复杂RAG策略（Rerank、多路召回融合），再考虑引入。

---

## 4. 数据模型概要

```sql
-- 当前核心表

knowledge_bases -- 知识库 (id, name, description, created_at, updated_at)
field_dicts     -- 字段字典 (id, kb_id, field_name, display_name, data_type, enum_values, description)
business_rules  -- 业务规则 (id, kb_id, rule_name, rule_type[hard|soft], expression, description, source)
state_machines  -- 状态机 (id, kb_id, entity, from_state, to_state, condition)
term_mappings   -- 术语映射 (id, kb_id, ui_term, tech_field, mapping_desc)
prd_documents   -- PRD文档 (id, kb_id, filename, file_format, raw_text, chunk_count, created_at)
defect_records  -- 缺陷记录 (id, kb_id, title, severity, root_cause, description, related_case, occurred_at, created_at)
test_cases      -- 用例 (id, title, priority, precondition, steps, expected_result, source[manual|ai], quality_score, knowledge_refs[json], batch_id, req_text, edited, edited_at, created_at)
review_records  -- 审核记录 (id, case_id, status[approved|rejected], reject_reason, reviewer_comment, reviewed_at)
llm_usage       -- LLM token 流水 (id, stage[clarify|module_split|generate|review|supplement], model, prompt_tokens, completion_tokens, reasoning_tokens, total_tokens, batch_id, created_at)
                -- 一行 = 一次 chat/completions 请求（含续写每轮、各并行模块各一行）。
                -- reasoning_tokens 已含在 completion_tokens 内，单独存一份用于判断高 reasoning_effort 值不值。
                -- batch_id 可空：clarify 阶段没有批次；生成任务结束拿到 batch_id 后回填，供批次级消耗展示。
                -- 不挂外键：调用与用例不是一对一（拆分/评审/补充都不直接产出某条用例）。

-- ChromaDB collections:
--   prd_docs         -- PRD文档分块向量化
--   historical_cases -- 历史用例向量化
--   defect_records   -- 缺陷描述向量化
```

---

## 5. 分阶段实施路径

### Phase 1：骨架 + 知识库CRUD ✓ 已完成
- Vue 3 + Element Plus + Pinia 项目搭建，纯 CSS flexbox 布局
- FastAPI + SQLite 建表（10张表），ChromaDB 预留
- 项目管理CRUD + 7种知识类型完整管理界面
- PRD文档上传解析（PDF/Word/MD/TXT）+ 模块关联
- 缺陷记录手动创建 + Excel批量导入（自动列映射+优先级转换）
- 模块级筛选（PRD/缺陷按模块过滤，为生成时模块级检索做准备）

**产出**：前后端联调通过的 Web 应用，Docker 部署方案齐备

### Phase 2：向量化 + 混合检索引擎 ✓ 已完成
- ChromaDB 向量存储 + sentence-transformers 本地 embedding
- 文本分块工具（chunk + overlap）
- 混合检索引擎（关键词SQL LIKE + ChromaDB语义搜索）
- `/retrieve` API：输入查询文本 → 返回匹配的字段/规则/PRD片段/缺陷
- 模块级检索：支持 module_ids 参数限定范围

**产出**：可在页面上录入和管理知识库，可检索

### Phase 3：用例生成引擎（2-3天）
- 需求输入界面（文本框 + 粘贴区域）
- Prompt构造器（三层结构 + 约束注入）
- LLM API集成（OpenAI 兼容，流式响应）
- 用例结果展示（表格 + JSON视图）
- 知识追溯标签展示

**产出**：输入需求 → 生成用例 → 查看结果

### Phase 4：审核闭环 + 导出（1-2天）
- 审核标注界面（逐条审核 + 归因）
- 统计面板（可用率/幻觉分布/覆盖度）
- Excel导出（兼容模板格式）
- 归因引导——不可用例自动提示补充什么知识

**产出**：审核 → 归因 → 补知识 → 再生 → 质量提升的完整闭环

---

## 6. 关键设计决策

### 6.1 向量检索 + 关键词检索的混合策略
- **关键词匹配**：用于字段字典、业务规则、状态机——这些是精确匹配场景，"订单状态"不能模糊成"订单情况"
- **语义检索**：用于历史用例——"下单失败"应能匹配到"支付异常"的用例，用向量相似度
- **混合权重**：关键词结果优先展示，语义结果作为few-shot补充

### 6.2 知识条目引用追溯
每条AI生成的用例在Prompt阶段要求LLM输出 `knowledge_refs` 字段：
```json
{
  "title": "VIP用户下单免运费",
  "knowledge_refs": [
    {"type": "business_rule", "id": "br_001", "name": "VIP免运费"},
    {"type": "field_dict", "id": "fd_012", "name": "freight_amount"}
  ]
}
```
这样审核时用户能快速定位"这条用例的依据是对是错"。

### 6.3 约束检查层（生成前自动校验）
在构造Prompt之前，系统可以自动做两层检查：
1. **字段存在性检查**：需求中提到的字段是否在字段字典中？
2. **状态可达性检查**：如果需求涉及状态变更，是否在状态机中存在合法路径？

如果检查不通过，先生成"知识缺失预警"，提示用户先补充知识再用例生成。

### 6.4 生成解耦：后台任务 + 前端状态提升
长任务生成不绑定 HTTP 请求与页面组件——后端用 `asyncio.create_task` + 独立 DB 会话跑后台任务并缓存 SSE 事件，前端把生成状态提升到 Pinia store。两者共同保证「切页面/刷新/重连」都能断点续看，详见 2.2「生成任务的可靠性设计」。

---

*设计方案版本 v0.1 | 后续实施细节见 /plan 输出*
